# Tests

- `bateman_cases.json` — validation cases for the decay-chain calculator (textbook and hand-computed;
  each with its source). Run with `npm test`.
- Link and accessibility checks run as part of the build.
